export type Data = {
  totalValue: number;
  monthMostOpen: string; // Mes com mais parcelas em aberto
  totalOpenValue: number;
  totalContracts: number;
  monthMostOpenValue: number; // Valor em aberto no mes com mais parcelas em aberto
};
